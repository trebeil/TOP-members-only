<h1>Posts</h1>

<%= link_to "New post", new_post_path %>

<% if @posts.none? %>
  <div>There are no posts yet.</div>
<% else %>
  <% @posts.each do |post| %>
    <h2> <%= post.title %> </h2> 
    <p> <%= post.body %> </p>
    <% if user_signed_in? %>
      <div> Author: <%= User.find_by(id: post.user_id).email %> </div>
    <% end %>
  <% end %>
<% end %>
